<h1>Listing offers_titles</h1>

<table>
  <tr>
    <th>Offer</th>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @offers_titles.each do |offers_title| %>
  <tr>
    <td><%= offers_title.offer_id %></td>
    <td><%= offers_title.title_id %></td>
    <td><%= link_to 'Show', offers_title %></td>
    <td><%= link_to 'Edit', edit_offers_title_path(offers_title) %></td>
    <td><%= link_to 'Destroy', offers_title, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Offers title', new_offers_title_path %>
