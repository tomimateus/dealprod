<%= form_for(@newsletter_frequency) do |f| %>
  <% if @newsletter_frequency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsletter_frequency.errors.count, "error") %> prohibited this newsletter_frequency from being saved:</h2>

      <ul>
      <% @newsletter_frequency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :days %><br />
    <%= f.number_field :days %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
